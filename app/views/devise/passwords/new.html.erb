<% provide(:title, "Passwort vergessen?") %>

<p>Keine Sorge! Wir schicken dir eine E-Mail mit einem Link, über den du ein neues Passwort einrichten kannst.</p>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name),
 html: { class: "form-horizontal", method: :post }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :email, required: true, autofocus: true %>

  <div class="controls">
    <%= f.button :submit, "Link anfordern", class: "btn-primary" %>
  </div>
<% end %>

<hr />

<%= render "devise/shared/links" %>

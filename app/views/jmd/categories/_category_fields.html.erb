<%= render 'shared/error_messages', object: f.object %>

<%= f.input :name %>

<%= f.input :slug %>

<%= f.input :solo %>

<%= f.input :ensemble %>

<%= f.input :genre, collection: JUMU_GENRES,
  label_method: lambda { |genre| name_for_genre(genre) },
  include_blank: false
  %>

<%= f.input :max_round, collection: @max_rounds,
  label_method: lambda { |round| name_for_round(round) },
  prompt: "Bitte wählen"
  %>

<%= f.input :official_min_age_group, collection: JUMU_AGE_GROUPS,
  include_blank: false
  %>

<%= f.input :official_max_age_group, collection: JUMU_AGE_GROUPS,
  include_blank: false
  %>

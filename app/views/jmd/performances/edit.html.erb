<% provide(:title, "Vorspiel bearbeiten") %>

<%= render 'noscript' %>

<%= simple_form_for([:jmd, @performance], html: { class: "form-horizontal" }) do |form| %>
  <%= render 'performance_fields', { f: form, show_hints: false } %>
  <div class="form-actions">
    <%= form.button :submit, "Änderungen speichern", class: "btn-primary" %>
    <%= link_to "Abbrechen", jmd_contest_performances_path(@performance.contest), class: "btn btn-default" %>
  </div>
<% end %>

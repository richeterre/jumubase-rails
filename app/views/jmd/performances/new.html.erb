<% provide(:title, "Neues Vorspiel erstellen") %>

<%= render 'noscript' %>

<%= simple_form_for([:jmd, @contest, @performance], html: { class: "form-horizontal" }) do |form| %>
  <%= render 'performance_fields', { f: form, include_stage_time: false } %>
  <div class="form-actions">
    <%= form.button :submit, "Vorspiel erstellen", class: "btn-primary" %>
    <%= link_to "Abbrechen", :back, class: "btn" %>
  </div>
<% end %>

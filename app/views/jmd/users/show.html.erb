<% provide(:title, @user.full_name) %>

<%= link_to "Alle Benutzer anzeigen", jmd_users_path, class: "btn" %>

<dl>
  <dt>E-Mail</dt>
    <dd><%= @user.email %></dd>
  <dt>Admininistrator</dt>
    <dd><%= @user.admin ? "Ja" : "Nein" %></dd>
  <dt>Rechte für</dt>
    <dd>
      <% if @user.admin %>
        alle Schulen
      <% else @user.hosts.each do |h| %>
          <%= h.name %><br />
        <% end %>
      <% end %>
    </dd>
</dl>

<% provide(:title, "Anmeldung bearbeiten") %>

<%= render 'noscript' %>

<%= simple_form_for(@performance, html: { class: "form-horizontal" }) do |form| %>
  <%= render 'performance_fields', { f: form, allow_contest_selection: false, show_hints: true } %>
  <div class="form-actions">
    <%= form.button :submit, "Änderungen speichern", class: "btn-primary" %>
  </div>
<% end %>

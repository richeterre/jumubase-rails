<h2>Anmeldung bearbeiten</h2>

<%= render 'noscript' %>

<%= simple_form_for(@performance, html: { class: "form-horizontal" }) do |form| %>
  <%= render 'performance_fields', :f => form %>
  <div class="form-actions">
    <%= form.button :submit, "Änderungen speichern", class: "btn-primary" %>
  </div>
<% end %>
